TASK:
  - match:
      1
    set_size:
      4
  - match:
      1
    set_size:
      6
  - match:
      1
    set_size:
      8
  - match:
      2
    set_size:
      4
  - match:
      2
    set_size:
      6
  - match:
      2
    set_size:
      8

MATCHES: [1,2]

MATCHES_STR:
  {"1": Match IN, "2": Mismatch OUT, "all": Match ALL}

SET_SIZES: [4,6,8]
